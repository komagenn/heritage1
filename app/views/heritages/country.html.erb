<div class=country-index>
 <h2 class="country-css"><%=@country.name %></h2>
  <ul>
    <% @heritages.each do |heritage| %>
      <li>
          <%= link_to "#{heritage.name}", "http://localhost:3000/heritages/country?q%5Bcountry_id_eq%5D=#{heritage.country.id}&commit=%E6%A4%9C%E7%B4%A2" %>  
           <%# <%= render partial: 'heritages/heritage',collection: @heritage %> 
           <%= link_to image_tag(heritage.images[0], class: :country_img ), heritage_path(heritage.id) %>
          <div class="country_index">
            <p class="country_summary">
           
            <%= link_to "#{heritage.register.name}年", heritage_path(heritage.id),class: :country__title %> 
            
          </div>
      </li>
    <% end %>
  </ul>
</div>
